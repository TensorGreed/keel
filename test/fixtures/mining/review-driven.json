{
  "name": "decision that emerges from review discussion",
  "pr": {
    "number": 5,
    "title": "Add input parser",
    "body": "Parses the config file.",
    "author": "carol",
    "state": "closed",
    "merged": true,
    "url": "https://github.com/o/r/pull/5",
    "files": ["src/parser.ts"],
    "reviews": [{ "author": "dave", "state": "CHANGES_REQUESTED", "body": "The field can be null in practice." }]
  },
  "comments": [
    {
      "id": 20,
      "prNumber": 5,
      "inReplyTo": null,
      "path": "src/parser.ts",
      "diffHunk": "@@ const name = config.name.trim()",
      "body": "config.name can be null when the key is omitted — this will throw.",
      "author": "dave"
    },
    {
      "id": 21,
      "prNumber": 5,
      "inReplyTo": 20,
      "path": "src/parser.ts",
      "diffHunk": "@@ const name = config.name.trim()",
      "body": "Good catch — guarding with a null check and defaulting to empty string.",
      "author": "carol"
    }
  ],
  "modelOutput": "{\"hasDecision\": true, \"summary\": \"Guard the parser against a null config field\", \"rationale\": \"A reviewer noted config.name can be null when omitted, which would throw; added a null check defaulting to empty string\", \"alternatives\": [], \"files\": [\"src/parser.ts\"], \"confidence\": \"medium\"}",
  "gold": {
    "hasDecision": true,
    "summary": "Guard the parser against a null config field",
    "rationale": "A reviewer noted config.name can be null when omitted, which would throw; added a null check defaulting to empty string",
    "alternatives": [],
    "files": ["src/parser.ts"],
    "confidence": "medium"
  }
}
